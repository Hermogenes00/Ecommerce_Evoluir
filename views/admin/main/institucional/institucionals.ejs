<%- include('../../../partials/header') %>
    <%- include('../../../partials/navbaradmin') %>

        <div class="container mt-3">
            <div class="row justify-content-md-center">
                <div class="input-group col-md-6 mb-3">
                    <input id="buscar" name="buscar" type="text" class="form-control form-control-sm"
                        placeholder="Busque uma postagem pelo título">
                    <div class="input-group-append">
                        <btn class="btn btn-outline-secondary btn-sm" onclick="filtrar(event)" id="btnBuscar" name="btnBuscar">
                            Buscar</a>
                    </div>
                </div>
            </div>
            <div class="row">
                <a href="/admin/institucional" class=" mt-3 btn btn-sm btn-primary">
                    Criar postagem
                </a>
            </div>
        </div>

        <div class="container mt-3">

            <% if(institucionals.length>0){%>
                <div class="row justify-content-md-center">
                    <%institucionals.forEach(i=>{%>
                        <div class="col-md-12 mb-3">
                            <div class="card text-center shadow-sm">
                                <a href="/admin/institucional/find/<%=i.slug %>" class="text-muted h5 mt-2"
                                    style="text-decoration: none;">
                                    <%=i.titulo%>
                                </a>
                            </div>
                        </div>
                        <% }) %>
                </div>
                <%} %> 
            

        </div>




        <script>
            function filtrar(event) {
                if (document.getElementById('buscar').value) {
                    window.location.href = `http://localhost:8090/admin/institucionals/${document.getElementById('buscar').value}`
                }
            }

        </script>

        <%- include('../../../partials/footer') %>